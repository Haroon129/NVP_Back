services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    environment:
      PORT: "5001"
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./data:/app/data
      - ./datos:/app/datos
    restart: unless-stopped
